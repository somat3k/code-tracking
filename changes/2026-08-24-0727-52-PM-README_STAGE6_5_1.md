# PITBULL Stage 6.5.1 • Linear Helper Order Fix

This is a compiler-order correction only.

Pine's linear compiler encountered the Stage 6.4 Wedge renderer before the declarations of:

- `fVRRemoteGeometry()`
- `fVRCard()`

Both helpers have now been moved before the first renderer that consumes them.

Stage 6.5 last-valid measurement retention remains unchanged:
- Wedge
- Fork
- Fibonacci
- Cement
- Dimensional Field / Spiral
- Cycle Orbit
- Pivot Angle
- Time Theory

No library API changed. Compile only the corrected master.