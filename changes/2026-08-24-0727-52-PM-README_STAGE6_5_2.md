# PITBULL Stage 6.5.2 • Global Orbit State Fix

This patch fixes CE10088 without changing the visual methodology.

The one-use `fRenderCycleOrbit()` wrapper has been removed. Cycle Orbit is now a global linear renderer, which is a better fit for Pine: