# Stage 4.8.2 • Render Return-Type Fix

This patch addresses CE10235 caused by mixed inferred return types inside
render-wrapper functions.

## Rule now enforced

Every `fRender*()` function ends with: